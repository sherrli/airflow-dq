test_name: Test
check_description: This is a description of the test and what it does.
threshold:
    eval_threshold: False
    min_threshold: 10
    max_threshold: 15
    threshold_conn_type: postgres
    threshold_conn_id: test_id
fields:
  conn_id: test_conn
  conn_type: postgres
  sql: SELECT AVG(value) FROM test;